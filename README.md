# lockdock
A MacOS utility tool to fix the macOS Dock to a single monitor when using multiple displays.

# Setup

## Make executable and move to your PATH
chmod +x dock-lock
sudo mv dock-lock /usr/local/bin/dock-lock
